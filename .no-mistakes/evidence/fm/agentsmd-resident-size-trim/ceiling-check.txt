### 1. Resident-size ceiling: AGENTS.md trimmed and under budget

$ git show 290df28b:AGENTS.md | wc -c   # base
83871
$ wc -c < AGENTS.md                     # after trim
77626

$ bin/fm-agentsmd-size.sh
AGENTS.md: 77626 bytes (ceiling: 102400)
exit=0

$ bin/fm-agentsmd-size.sh --help
Usage: bin/fm-agentsmd-size.sh
Assert AGENTS.md stays under a resident-size ceiling.
Exit 0 if within budget, exit 1 with a diagnostic if over.
exit=0

$ bin/fm-agentsmd-size.sh AGENTS.md     # stray argument refused
fm-agentsmd-size: unexpected argument 'AGENTS.md'; this check takes no arguments
Usage: bin/fm-agentsmd-size.sh
Assert AGENTS.md stays under a resident-size ceiling.
Exit 0 if within budget, exit 1 with a diagnostic if over.
exit=2

### 2. Over-ceiling path (padded copy, restored afterwards)
$ bash tests/fm-agentsmd-size.test.sh
ok - passes when AGENTS.md (77626 bytes) is under the 100KB ceiling
ok - fails (1) with diagnostic when AGENTS.md (108202 bytes) exceeds 100KB
ok - fails when AGENTS.md is missing
ok - output includes both actual size (77626) and ceiling (102400)
exit=0

### 3. Gate selects the ceiling check when AGENTS.md changes
$ bin/fm-test-run.sh --list --changed | grep agentsmd   # only AGENTS.md dirty
tests/fm-agentsmd-size.test.sh
