=== pt-tracker ship brief (REPO='sKeLe100/pt-tracker.git') — law appears in the brief the crewmate is launched with ===
   re-enters the same loop, and reporting the loop early is cheaper than a stale-session rescue.

# Firstmate instruction inbox
Firstmate steers you through durable message files in '/tmp/ptev/a/state/demo-ship.inbox'.
When a terminal message says an instruction is waiting there - and at any natural checkpoint when you are unsure - list '/tmp/ptev/a/state/demo-ship.inbox'/*.msg, read and act on each message in numeric order, then acknowledge each handled message by moving it: `mv '/tmp/ptev/a/state/demo-ship.inbox'/NNN.msg '/tmp/ptev/a/state/demo-ship.inbox'/handled/`.
The move IS the acknowledgement: without it firstmate rings again and eventually treats you as stuck. An empty or absent inbox needs no action.

# pt-tracker tracker-entry law
Before any work begins on pt-tracker, you MUST complete BOTH of the following:
1. Use the ROADMAP execution prompt in the pt-tracker repo to plan and design the work.
2. Create a CURRENT.md tracker entry in the pt-tracker repo's planning surfaces.
This is a pre-dispatch precondition, not a post-dispatch step. Do not start implementation
until both items above are complete and recorded in CURRENT.md.

# Project memory

=== non-pt-tracker ship brief (REPO='some-repo'), same region — no law, no blank-line drift ===

# Firstmate instruction inbox
Firstmate steers you through durable message files in '/tmp/ptev/a/state/demo-other.inbox'.
When a terminal message says an instruction is waiting there - and at any natural checkpoint when you are unsure - list '/tmp/ptev/a/state/demo-other.inbox'/*.msg, read and act on each message in numeric order, then acknowledge each handled message by moving it: `mv '/tmp/ptev/a/state/demo-other.inbox'/NNN.msg '/tmp/ptev/a/state/demo-other.inbox'/handled/`.
The move IS the acknowledgement: without it firstmate rings again and eventually treats you as stuck. An empty or absent inbox needs no action.

# Project memory
If `AGENTS.md` or `CLAUDE.md` already exists, or if this task produced durable project-intrinsic knowledge, run `/home/sean_/.no-mistakes/worktrees/4936102b4bcf/01M1M7A0SF9ZGYXEMTEJRKA00M/bin/fm-ensure-agents-md.sh .` in the worktree.
Record only project knowledge useful to almost every future session.

=== non-pt-tracker brief vs base commit 58cb216 output (paths normalized) ===
no differences — non-pt-tracker briefs byte-identical to pre-change output

=== pt-tracker scout brief: occurrences of 'tracker-entry law' ===
0
=== repo-spelling matrix: law present (1) for each spelling ===
pt-tracker                     law_blocks=1
sKeLe100/pt-tracker            law_blocks=1
pt-tracker.git                 law_blocks=1
/home/x/repos/pt-tracker       law_blocks=1
some-repo                      law_blocks=0
